name: widget_test_pokedex
description: A new Flutter project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.6.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.3.2
  build_runner: ^2.4.15

  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
